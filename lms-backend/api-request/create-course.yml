info:
  name: create-course
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:3000/api/courses
  body:
    type: json
    data: |-
      {
        "title": "dummy course 3",
        "description": "for testing course",
        "thumbnail": "thumbnail3.png"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
